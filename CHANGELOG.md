# Changelog

Per-package changelogs are generated by changesets into each package directory. This file records milestones.

## Unreleased

- Initial scaffold: core engine (profiles, vault, adapters, router, events), local proxy, Electron bridge, React switcher, CLI, docs and CI.
