# Changesets

Every user-visible change ships with a changeset: `pnpm changeset`, pick the packages, write one line.
The release workflow turns accumulated changesets into version bumps and CHANGELOG entries.
